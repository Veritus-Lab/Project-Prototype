# Task 20 — Webhooks e sincronização

Endpoint `/api/webhooks/asaas` preparado com validação fail-closed do token, payload mínimo sanitizado e idempotência pelo evento externo. A ativação e o teste de assinatura permanecem pendentes da credencial oficial do Asaas.
