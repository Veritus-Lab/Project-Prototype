"use client";import{useState}from'react';export function InterestForm(){const[s,setS]=useState('');async function send(f:FormData){const r=await fetch('/api/interest',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(f))});setS(r.ok?'Recebemos seu interesse. A FLERNK entrará em contato.':'Revise os dados e tente novamente.')}return <form action={send} className="invitation-form"><input className="input" name="name" placeholder="Seu nome" required/><input className="input" name="phone" placeholder="WhatsApp" required/><input className="input" name="email" placeholder="E-mail (opcional)" type="email"/><button className="button button-primary">Quero conhecer a FLERNK</button>{s?<p className="field-hint">{s}</p>:null}</form>}
