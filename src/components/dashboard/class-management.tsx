"use client";
import { useActionState } from "react";
import { Button } from "@/components/ui/button";
import { addClassMemberAction, cancelClassMeetingAction, createClassAction, createClassMeetingAction } from "@/app/(dashboard)/treinador/calendario/actions";
type ClassRow = { id: string; name: string };
type Meeting = { id: string; class_id: string; starts_at: string; ends_at: string; canceled_at: string | null; cancellation_reason: string | null };
type Student = { id: string; name: string; enrollment: { id: string; status: "active" | "suspended" | "ended" } | null };
type Membership = { id: string; class_id: string; student_id: string; enrollment_id: string; student_name: string };
type State = { error?: string; success?: boolean };
const initial: State = {};
export function ClassManagement({ classes, meetings, students, memberships }: { classes: ReadonlyArray<ClassRow>; meetings: ReadonlyArray<Meeting>; students: ReadonlyArray<Student>; memberships: ReadonlyArray<Membership> }) {
  const [classState, classAction, creatingClass] = useActionState(createClassAction, initial);
  const [meetingState, meetingAction, creatingMeeting] = useActionState(createClassMeetingAction, initial);
  const [memberState, memberAction, addingMember] = useActionState(addClassMemberAction, initial);
  const availableStudents = students.filter((student) => student.enrollment && student.enrollment.status !== "ended");
  return <><div className="training-form-grid"><form action={classAction} className="invitation-form"><h3>Nova turma</h3><input className="input" name="name" placeholder="Nome da turma" required /><textarea className="input" name="description" placeholder="Descrição (opcional)" rows={2} /><Button type="submit" disabled={creatingClass}>{creatingClass ? "Criando..." : "Criar turma"}</Button>{classState.error ? <p className="form-error" role="alert">{classState.error}</p> : null}</form>{classes.length ? <form action={meetingAction} className="invitation-form"><h3>Agendar recorrência semanal</h3><select className="input" name="classId" required>{classes.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select><input className="input" name="startsAt" type="datetime-local" required /><input className="input" name="endsAt" type="datetime-local" required /><label>Ocorrências<input className="input" name="occurrences" type="number" min={1} max={52} defaultValue={1} required /></label><Button type="submit" disabled={creatingMeeting}>{creatingMeeting ? "Agendando..." : "Agendar"}</Button>{meetingState.error ? <p className="form-error" role="alert">{meetingState.error}</p> : null}</form> : null}{classes.length && availableStudents.length ? <form action={memberAction} className="invitation-form"><h3>Vincular aluno</h3><select className="input" name="classId" required>{classes.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select><select className="input" name="studentId" required>{availableStudents.map((student) => <option key={student.id} value={student.id}>{student.name}</option>)}</select><select className="input" name="enrollmentId" required>{availableStudents.map((student) => student.enrollment ? <option key={student.enrollment.id} value={student.enrollment.id}>{student.name}</option> : null)}</select><Button type="submit" disabled={addingMember}>{addingMember ? "Vinculando..." : "Vincular"}</Button>{memberState.error ? <p className="form-error" role="alert">{memberState.error}</p> : null}</form> : null}</div><section><h3>Alunos vinculados</h3>{memberships.length ? <ul className="dashboard-list">{memberships.map((membership) => <li key={membership.id}><span className="dashboard-list-title">{membership.student_name}</span><span className="dashboard-list-detail">{classes.find((item) => item.id === membership.class_id)?.name ?? "Turma"}</span></li>)}</ul> : <p className="dashboard-empty-state">Nenhum aluno vinculado.</p>}</section><ul className="dashboard-list">{meetings.map((meeting) => <li key={meeting.id}><span className="dashboard-list-title">{classes.find((item) => item.id === meeting.class_id)?.name ?? "Turma"}</span><span className="dashboard-list-detail">{new Intl.DateTimeFormat("pt-BR", { dateStyle: "short", timeStyle: "short" }).format(new Date(meeting.starts_at))} · {meeting.canceled_at ? `Cancelado: ${meeting.cancellation_reason}` : "Agendado"}</span>{!meeting.canceled_at ? <form action={cancelClassMeetingAction} className="enrollment-inline-form"><input type="hidden" name="meetingId" value={meeting.id} /><input className="input" name="reason" placeholder="Motivo do cancelamento" required /><Button type="submit" variant="secondary">Cancelar</Button></form> : null}</li>)}</ul></>;
}
