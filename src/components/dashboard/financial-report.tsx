export function FinancialReport({data}:{data:{balance:number;received:number;paid:number;pending:number;planned:number}}){const f=(v:number)=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(v/100);return <section className="class-overview"><h3>Resumo financeiro</h3><div className="class-overview-grid">{[['Saldo realizado',data.balance],['Recebido',data.received],['Despesas realizadas',data.paid],['Cobranças pendentes',data.pending],['Movimentações previstas',data.planned]].map(([l,v])=><div className="class-summary-item" key={String(l)}><span className="class-summary-value">{f(v as number)}</span><span className="class-summary-label">{l}</span></div>)}</div></section>}
