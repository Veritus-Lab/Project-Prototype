export function PaymentIntegrationStatus({configured,reason}:{configured:boolean;reason:string|null}){return <section className="class-overview"><h3>Pagamento online</h3><p className="field-hint">{configured?'Asaas sandbox configurado. O checkout pode ser ativado após a validação técnica.':reason}</p><span className={configured?'status-badge status-badge-success':'status-badge'}>{configured?'Pronto para validação':'Integração pendente'}</span></section>}
